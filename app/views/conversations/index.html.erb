<div class="container mt-4">
  <%= link_to "← Voltar para a página inicial", root_path, class: "btn btn-link p-0 mb-3" %>
  <h1 class="mb-3">Meus chats</h1>

  <% if @conversations.any? %>
    <ul class="list-group">
      <% @conversations.each do |conversation| %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <div>
            <strong><%= conversation.title.presence || "Chat sem título" %></strong><br>
            <small>
              Criado em: <%= conversation.created_at.strftime("%d/%m/%Y %H:%M") %>
            </small>
          </div>
          <%= link_to "Abrir", conversation_path(conversation), class: "btn btn-sm btn-primary" %>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p>Você ainda não tem chats. Vá para a página inicial e clique em "Começar chat com a IA".</p>
  <% end %>
</div>
