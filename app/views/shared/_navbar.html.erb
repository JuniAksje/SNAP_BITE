<nav class="navbar navbar-expand-sm navbar-custom">
  <div class="container-fluid d-flex justify-content-between align-items-center">

    <!-- COLUNA ESQUERDA (vazia pra ajudar no centramento) -->
    <div class="navbar-left"></div>

    <!-- COLUNA CENTRAL (logo) -->
    <div class="navbar-brand navbar-logo-container">
      <%= link_to "#", class: "navbar-brand navbar-logo" do %>
        <%= image_tag "snapbite.PNG", class: "navbar-logo-img" %>
      <% end %>
    </div>

    <!-- COLUNA DIREITA (links Home / Conta) -->
    <div class="navbar-collapse collapse show navbar-right" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto">

        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Home", restaurants_path, class: "nav-link nav-link-custom" %>
          </li>

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle nav-link-custom"
               href="#"
               id="navbarDropdown"
               role="button"
               data-bs-toggle="dropdown"
               aria-expanded="false">
              Conta
            </a>

            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <li>
                <%= link_to "Log out", destroy_user_session_path,
                            data: { turbo_method: :delete },
                            class: "dropdown-item" %>
              </li>
            </ul>
          </li>

        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link nav-link-custom" %>
          </li>
        <% end %>

      </ul>
    </div>

  </div>
</nav>
